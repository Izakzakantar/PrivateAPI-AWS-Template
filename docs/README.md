TODO: Add the architecture diagram image here.

This template's root README.md references an image at:

    docs/architecture-diagram.png

That file does not exist yet and cannot be generated automatically -- add
your own diagram of the request path described in the main README (Client
-> Route 53 -> CloudFront -> CloudFront VPC Origin -> Internal ALB -> VPC
Endpoint -> Private API Gateway -> Lambda -> DynamoDB) and save it here as
`architecture-diagram.png`.

Once the file is added, remove this placeholder note.
